import { type FunctionalComponent as FC } from 'vue';
import { RouterView } from 'vue-router';

const App: FC = () => {
    return (
        <>
            <RouterView />
        </>
    );
};

export default App;
