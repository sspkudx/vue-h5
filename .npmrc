# pnpm 11+ 仅从 .npmrc 读取 registry/auth 配置；
# 其余设置（shamefullyHoist / engineStrict 等）请配置在 pnpm-workspace.yaml。
# 华为云
registry=https://mirrors.huaweicloud.com/repository/npm/
# 腾讯云
# registry=https://mirrors.cloud.tencent.com/npm/
