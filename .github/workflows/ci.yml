name: CI

on:
    push:
        branches: [main]
    pull_request:

jobs:
    lint-test-build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            # pnpm/action-setup 在 setup-node 之前：保证 pnpm 进入 PATH，
            # setup-node 的 cache: pnpm 才能定位 pnpm store 并正确缓存
            # （版本取自 packageManager 字段，corepack 对齐）
            - uses: pnpm/action-setup@v4
              with:
                  run_install: false

            - uses: actions/setup-node@v4
              with:
                  node-version-file: .node-version
                  cache: pnpm

            - name: 安装依赖
              run: pnpm install --frozen-lockfile

            - name: ESLint
              run: pnpm lint

            - name: Stylelint
              run: pnpm lint:style

            - name: Prettier 检查
              run: pnpm format:check

            - name: 文档/模板漂移检查（README/docs/技能模板 vs 真实代码）
              run: pnpm check:docs

            - name: 单元测试
              run: pnpm test

            - name: 完整构建（packages + apps）
              run: pnpm build

    e2e:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - uses: pnpm/action-setup@v4
              with:
                  run_install: false

            - uses: actions/setup-node@v4
              with:
                  node-version-file: .node-version
                  cache: pnpm

            - name: 安装依赖
              run: pnpm install --frozen-lockfile

            - name: 安装 Playwright 浏览器（chromium）
              run: pnpm exec playwright install --with-deps chromium

            - name: E2E 冒烟测试（example-app，移动端 viewport）
              run: pnpm test:e2e

            - name: 上传失败报告（如有）
              if: failure()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-report
                  path: playwright-report/
                  retention-days: 7
